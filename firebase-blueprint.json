{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Stores user profile and subscription status",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "username": { "type": "string", "description": "Unique username" },
        "displayName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin", "parent", "student", "wydawca"] },
        "schoolClass": { "type": "string", "enum": ["4sp", "5sp", "6sp", "7sp", "8sp", "egzamin_8", "1pp", "2pp", "3pp", "4pp", "5pp", "matura"] },
        "selectedLevels": { "type": "array", "items": { "type": "string" }, "description": "Levels/classes the parent is interested in" },
        "isPremium": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" },
        "streak": { "type": "number" },
        "lastActiveDate": { "type": "string", "format": "date" },
        "totalTimeSpent": { "type": "number", "description": "Total time in minutes" },
        "parentEmail": { "type": "string", "format": "email" },
        "parentUid": { "type": "string", "description": "UID of the parent for student accounts" },
        "childrenUids": { "type": "array", "items": { "type": "string" }, "description": "List of children UIDs for parent accounts" },
        "weakTopics": { "type": "array", "items": { "type": "string" } },
        "totalPoints": { "type": "number" },
        "notificationFrequency": { "type": "string", "enum": ["none", "daily", "weekly", "monthly"] },
        "alertOnMissingLogin": { "type": "boolean" },
        "completedStudyTopics": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["uid", "email", "role", "username"]
    },
    "Username": {
      "title": "Username Mapping",
      "description": "Ensures username uniqueness",
      "type": "object",
      "properties": {
        "uid": { "type": "string" }
      },
      "required": ["uid"]
    },
    "LessonProgress": {
      "title": "Lesson Progress",
      "description": "Tracks user progress in a specific lesson",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "lessonId": { "type": "string" },
        "status": { "type": "string", "enum": ["not-started", "in-progress", "completed"] },
        "score": { "type": "number" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "lessonId", "status"]
    },
    "Lesson": {
      "title": "Lesson Content",
      "description": "Stores the rich text content of a lesson",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "week": { "type": "number" },
        "topic": { "type": "string" },
        "scope": { "type": "array", "items": { "type": "string" } },
        "content": { "type": "string" },
        "videoUrl": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "topic", "content", "week"]
    },
    "Quiz": {
      "title": "Quiz",
      "description": "A set of questions for a lesson",
      "type": "object",
      "properties": {
        "lessonId": { "type": "string" },
        "title": { "type": "string" },
        "questions": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "question": { "type": "string" },
              "options": { "type": "array", "items": { "type": "string" } },
              "correctAnswer": { "type": "number" },
              "explanation": { "type": "string" }
            }
          }
        }
      }
    },
    "PublicProfile": {
      "title": "Public Profile",
      "description": "Non-sensitive user data for leaderboard and public display",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "totalPoints": { "type": "number" },
        "streak": { "type": "number" }
      },
      "required": ["uid", "displayName", "totalPoints"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profile documents"
    },
    "/usernames/{username}": {
      "schema": "Username",
      "description": "Unique username mapping"
    },
    "/public_profiles/{userId}": {
      "schema": "PublicProfile",
      "description": "Public user profile data"
    },
    "/users/{userId}/progress/{lessonId}": {
      "schema": "LessonProgress",
      "description": "User progress per lesson"
    },
    "/quizzes/{quizId}": {
      "schema": "Quiz",
      "description": "Quiz content"
    },
    "/lessons/{lessonId}": {
      "schema": "Lesson",
      "description": "Lesson content and metadata"
    },
    "/settings/{settingId}": {
      "schema": "object",
      "description": "Global application settings"
    }
  }
}
